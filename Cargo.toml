[package]
name = "divechain"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
rpassword = "7"

[target.'cfg(target_os = "macos")'.dependencies]
security-framework = "3"
